<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Splash theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary" tools:targetApi="LOLLIPOP">@color/primary</item>
        <item name="android:colorPrimaryDark" tools:targetApi="LOLLIPOP">@color/primary</item>
        <item name="android:colorAccent" tools:targetApi="LOLLIPOP">@color/accent</item>
        <item name="colorControlActivated">@color/accent</item>

        <item name="android:statusBarColor" tools:targetApi="LOLLIPOP">@color/primary</item>
        <item name="android:navigationBarColor" tools:targetApi="LOLLIPOP">@color/primary</item>

        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

</resources>
